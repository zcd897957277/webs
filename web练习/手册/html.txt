赵旭
zhaoxu@tedu.cn
问题描述
问题截图
源码打包
******************************************
				    第一天
1、WEB基础知识
   1、Internet
      全球性计算机互联网

      1、服务
         Telnet ：远程登录
	 Email ：邮件服务
	 WWW ：World Wide Web
	 BBS ：电子公告板(论坛)
	 FTP ：文件上传下载服务
      2、实现基本技术
         1、分组交换原理
	 2、TCP/IP 协议
   2、WEB
      1、什么是WEB
         就是运行在Internet上的一种应用程序-”网页“程序
      2、作用
         将 信息和服务 进行连接
   3、WEB的工作原理
      1、工作原理简介
         基于 浏览器/服务器 模式
	 浏览器/服务器 ：B/S
	   Browser / Server
	 客户端/服务器 ：C/S
	   Client / Server
      2、B/S
         由 WEB服务器(Server)、浏览器(Browser)、通信协议(HTTP)

	 Http:Hyper Text Transfer Protocol
	 http协议规范了数据是如何打包以及如何在互联网上进行传递的。

         服务器：一台电脑
	    PC：个人计算机
      3、WEB 服务器
         1、作用
	    1、存储web页面
	    2、响应浏览器请求，执行服务器程序
	    3、安全功能
	 2、服务器产品
	    1、TOMCAT
	    2、IIS
	    3、APACHE
      4、WEB浏览器
         1、作用
	    1、提交请求
	    2、解析HTML，CSS,JS
	    3、以 图形化 的方式进行显示
	 2、主流 浏览器 产品
	    IE
	    Firefox : 火狐
	    Chrome : Google
	    Opera : 欧朋
	    Safari : Apple
      5、WEB 相关技术
         1、服务器端技术
	    都能访问数据库

	    JSP
	    PHP
	    ASP
	    ASP.NET
	 2、客户端技术
	    运行在浏览器中的技术
	    HTML ：
	    CSS ：
	    Javascript ：
2、HTML入门
   1、HTML 概述
      1、什么是HTML
         Hyper Text Makrup Language
	 超文本标记语言

	 超文本 ：由一组特殊效果的文本来组成  
	    a : 首字符
	    <a> : 超链接

	    b : 
	    <b> : 加粗
	 标记：超文本的组成形式
	    <> : 标签

	 注意：在计算机中，是以 .html 或 .htm作为结尾的 文件

   2、html 语法
      1、标记(元素)
         标记 即 元素 ，组成html页面的最基本元素

	 html页面中，不同的标记会有不同的显示效果。

	 语法：
	   <>
	   封闭类型标记：
	     也叫做双标记：
	     <标记>内容</标记>
	     <a>百度</a>
	     <b>Google</b>
	     注意：有开始，必须有结束
	   非封闭类型标记：
	     只有开始即可，允许没有结束。
	     也称之为：单标记
	     <标记>或<标记/>
	     <br/> : 换行
	     <img/> : 图像
      2、标记(元素)的嵌套
         一个标记中又出现了另外的标记
	 <div>
	   <p></p>
	 </div>
	 注意：
	  1、嵌套顺序不要写错
	    <div>
	      <p>
	    </div>
	      </p>
	    以上是错误结构
	  2、缩进
	     Tab ：你	好
	     <div>
		     <p>
			     <span>
				<a>百度</a>
			     </span>
		     </p>
	     </div>
	  3、双标记 要写就写一对
	     <p>
		<span>
			
		</span>
	     </p>

	     <p>
	      hello world
	      <span>
	        你好，世界
	        </span>
		</p>
      3、属性 和 值 
         1、  
	   每个 标记都会有不同的属性，用来定义当前标记的状态
	 2、语法
	    <标记 属性=值 属性=值></标记>
	    align : 标签内文本的水平对齐方式
	      取值：
	        left : 左对齐
		right ：右对齐
		center ： 居中对齐
            <p align=center>Hello World</p>
	    <p align='center'>Hello World</p>
	    <p align="center">Hello World</p>

	    标准属性：所有标记都会具备的属性
	      id : 定义元素在页面中的唯一标识
	      title : 鼠标移入到当前元素上时所提示的文本
	      class : 定义当前元素的类样式
	      style:定义当前元素的行内样式
      4、注释
         1、作用
	    不会被浏览器所解析的内容
	 2、语法
	    <!--  注释内容  -->
	 3、注意
	    1、注释不能放在开始标记中
	      <p <!-- 注释内容 -->></p>
	      以上结构是错的
	    2、注释不能嵌套
	       <!--
	         <!--
		 
		 -->
	       -->
      5、HTML 和 XHTML
         1、W3C
	    World Wide Web Consortium
	    万维网联盟
	 2、HTML4.01
	    1999.12.24
	    <br> 或 <br/>
	 3、XHTML1.0
	    2000.1.26
	    <br/>
	 4、HTML5
	    要求不再严格，同时又能保证兼容性
	    1、单标记无所谓结束
	    2、对属性值得要求松散
	       <p align="center"></p>
	       <p align='center'></p>
	       <p align=center></p>
	       <input readonly>
   3、文档结构
      1、HTML文档结构
         1、文档类型声明
	    <!doctype html>
	 2、html页面
	    有 一对 html 根标记组成
	    <html></html>
      2、页面中的元素
         出现在 <html></html>根标记中的内容
	 1、<head></head>
	    头元素
	    作用：定义页面的全局信息
	 2、<body></body>
	    网页主体
      3、head 元素
         允许包含以下内容：
	 1、<title></title>定义网页标题
	 2、meta
	    1、编码格式定义
	       <meta charset="utf-8">
	    2、定义网页关键字
	       <meta name="keywords" content="关键字,关键字2,关键字3">
	    3、定义网页描述
	       <meta name="description" content="描述的内容">
3、文本标记
   1、文本标记
      通过 文本标记 可以设置 文本不同的显示效果
      1、特殊字符
         &nbsp; 空格
	 &lt; <   --> less than  
	 &gt; >   --> greater than
	 &copy; ? --> copyright
	 &yen; ￥
      2、文本标记
         1、文本样式
	    <b></b> --加粗
	    <i></i> --倾斜
	    <s></s> --删除线
	    <u></u> --下划线
	    <sub></sub> --下标
	    <sup></sup> --上标
	 2、标题元素
	    <hn></hn>
	    n:1-6数字
	 3、段落元素
	    <p></p>
	    注意：p元素只能嵌套文本、图像一级行内元素
	    p中不能嵌套p也不能嵌套 div
	 4、换行元素
	    <br>
	 5、块分区元素
	    <div></div>
	    作用：做网页的布局的。
	    注意：div可以嵌套任何一个元素
	 6、行分区元素
	    <span></span>
	    作用：做行内元素的布局和分区的
	 7、行内元素与块级元素
	    1、行内元素
	       多个元素在一行内显示
	       span,i,u,s,b,...
	       特点：修饰文本
	    2、块级元素
	       每个元素独占一行
	       div,p,hn,...
	       特点：做布局
	 8、分割线元素
	    <hr>
	    属性：
	      size:厚度
	      width:宽度
	      align:水平对齐方式
	      color:颜色
	 9、预格式化元素
	    保留文档中的 回车 和 空格 的效果
	    <pre></pre>
4、图像与链接
   1、URL
      1、目录
         存放文件的文件夹
	 每个目录下都可以包含若干子集目录
	 最顶层的目录可称之为 "主目录"
      2、URL
         1、什么是URL
	    Uniform Resource Locator
	    统一资源定位器
	    用于描述网络中"资源文件"位置的一种方式
	 2、URL的三种表现形式
	    1、绝对路径
	       1、访问网络资源
	          由 协议名、主机名(IP地址)、目录路径、文件名称

		  https://www.baidu.com/img/bd_logo1.png

		  协议名：http 或 https
		  主机名：IP地址，域名
		     www.xxx.com
		  目录路径：资源文件所在的文件夹路径
	       2、访问本机资源
	          从 文件所在的最高级 目录下一层一层查找

		  E:\Course\01_HTML5BASIC\03_SRC\COOKBOOK\WEB_HTML5BASIC_COOKBOOK_DAY01\images\page.JPG

		  缺点：程序一旦发生位置改变，要保证新计算机中也要有对应的绝对路径和文件。
	    2、相对路径
	       相对当前文件所在的位置，去查找资源文件的路径
	       1、同目录下
	          通过 文件 名称 引用文件
	       2、父目录下
	          先返回，再查找资源文件
		  ../ : 向上返回一级目录
	       3、子目录下
	          先进入到文件夹中，再查找资源文件
		  目录名/目录名/文件名

		注意：相对路径是通过 文件间的位置关系进行查找的。只要位置关系不变，任何资源文件都能找到。
	    3、根相对路径
	       永远都是从web站点的根目录处开始查找
	       以 / 作为开始的路径都是根相对路径
	       /img/a1.jpg
   2、图像
      1、图像格式
         1、jpeg(图形图像联合专家组)
	    表现形式:*.jpg  *.jpeg
	    特点：有损压缩
	 2、gif(图像接口格式) 
	    表现形式:*.gif
	    特点：动画
	 3、png(可移植网络图形)
	    表现形式:*.png
	    特点：背景为透明的时候使用
      2、图像元素
         1、语法
	    <img>
	 2、属性
	    1、src
	       引用图像的路径(绝对/相对/根相对)
	    2、width
	       宽度
	    3、height
	       高度

	       注意：如果宽度和高度只设置一个属性的话，那么图片将 等比缩放 
	    4、title
	       鼠标移入时所提示的文本
   3、链接
      1、什么是链接元素
         用户 点击 能够完成页面跳转的元素就是 链接。也称为 超链接
      2、语法
         <a>内容</a>
      3、属性
         1、href
	    链接URL(相对/绝对/根相对)
	 2、target
	    目标：即打开新网页的方式
	    取值：
	       _blank : 新标签页中打开
	       _self : 在自身标签页中打开(默认值)
      4、链接的表现形式
         1、目标文档为下载资源
	    链接目标为 *.rar 或 *.zip时就是下载操作
	 2、电子邮件链接
	    前提：必须有邮件客户端(Foxmail,Outlook)，绑定账号
	    <a href="mailto:目标邮件地址">联系我们</a>
	 3、返回页面顶部的空链接
	    <a href="#">返回顶部</a>
	 4、链接到 Javascript
	    即 执行JS操作
	    <a href="">执行JS</a>


******************************************
				    第二天


1、回顾
   1、HTML 基础语法
      1、标记
         即 元素 
	 语法：<标记>
	 分类：
	   1、封闭类型(双标记)
	      <标记></标记>
	      注意：有开始必须有有结束
	   2、非封闭类型(单标记)
	      <标记>或<标记/>

	      <hr>
	      <br>
	      <img>
	      <input>
	 嵌套：一个标记中出现另外的标记
	    1、嵌套顺序
	    2、缩进
	    3、不能随意嵌套
	       1、行内不能嵌套块
	       2、p不能嵌套块
	       3、非布局元素不要嵌套div
      2、属性 和 值
         作用：修饰元素
	 语法：<标记 属性="值" 属性="值">
	 标准属性：
	   id:定义元素唯一名称
	      1、布局时用
	      2、JS用
	   title : 鼠标移入时提示的文字
	   class : 定义元素的类选择器
	   style : 定义元素的行内(内联)样式
      3、注释
         <!-- 注释内容 -->
	 1、不能嵌套
	 2、不能出现在标记中
	    <标记  <!-- -->>
	    错误
      4、HTML 和 XHTML
         HTML4.01
	 XHTML : 更严格更纯净的html版本
	 HTML5 : 更简洁
   2、HTML 结构
      1、文档结构
         1、文档类型声明
	    <!doctype html>
	 2、html页面
	    由一对 html 根标记组成
      2、html 页面结构
         1、头元素 <head>
	    作用：定义全局信息
	    包含：
	      1、title
	         <title></title> 
		 定义页面标题
	      2、<meta charset="utf-8">
	         定义页面编码格式
	      3、<meta>
	        1、<meta name="keywords" content="关键字,关键字">
		2、<meta name="description" content="描述信息">
		只面向搜索引擎
              4、<script>
	         定义或引用Javascript
	      5、<style>
	         定义内部样式表
	      6、<link>
	         引入外部样式表
	 2、网页主体 <body>
	    显示的内容... ...
	    属性：
	       text : 定义页面文本颜色
	       bgcolor:定义页面背景颜色
   3、文本
      1、特殊字符
         1、&nbsp; 空格
	 2、&lt; <
	 3、&gt; >
	 4、&copy; ?
	 5、&yen; ￥
      2、文本标记
         1、文本样式(行内-display:inline)
	    <b></b>加粗
	    <i></i>斜体
	    <u></u>下划线
	    <s></s>删除线
	    <sub></sub>下标
	    <sup></sup>上标
         2、标题元素(块级：block)
	    <hn></hn>
	    n : 1-6
	    特点：
	       1、改变字体大小
	       2、加粗
	       3、上下垂直空白
	 3、段落
	    <p></p>
	    特点：
	       1、上下垂直空白
	 4、换行
	    <br>
	 5、块分区 - block
	    <div></div>
	    作用：布局
	 6、行分区 - inline
	    <span></span>
	    作用：包裹文本 并且 自定义样式
	 7、分割线元素
	    <hr> 或 <hr/>
	    属性：
	      size ：尺寸，厚度
	      width ：宽度
	      align ：水平对齐
	      color ：颜色
	 8、预格式化 - block
	    <pre></pre>
	    特点：保留源文档中的格式(回车，空格)
	    <pre>
		<p>Hello World</p>
	    </pre>
         9、行内元素与块级元素
	    1、行内
	       1、多个元素一行内显示
	       2、包裹文本处理样式
	    2、块级
	       1、每个元素独占一行
	       2、布局
   4、图像和链接
      1、URL
         Uniform Resource Locator
	 统一资源定位器
	 俗称：路径
         
	 分类：
	   1、绝对路径
	      特点：从一个固定位置处开始查找
	      1、网络资源
	         协议名
		 IP，主机名，域名
		 文件所在路径
		 文件名
	      2、本机资源
	         从文件所在的最高位置(盘符)处开始查找
	   2、相对路径
	      相对于文件所在的位置处开始查找资源文件的路径,主要依托于位置关系来查找
	      1、同目录
	         直接引用
	      2、子目录
	         先进入
	      3、父目录
	         先返回 
		 ../
	   3、根相对路径
	      从 web 站点的根目录开始
	      / 开始
      2、图像
         1、格式
	    jpg : 有损压缩，压缩比率大
	    gif : 动画
	    png : 背景透明
         2、语法
	    <img>
	    属性：
	      src ：图像所在URL
	      title ：鼠标移入显示的文字
	      width ：宽度
	      height：高度
      3、链接
         1、语法
	    <a>内容</a>
	 2、属性
	    1、href
	       链接URL
	    2、target
	       目标：打开新页面方式
	       取值：
	         _blank : 新标签页中打开
		 _self : 自身页面中打开
	    3、title
	    4、name
         3、表现形式
	    1、目标为下载的资源
	       href="*.rar"
	       href="*.zip"
	    2、电子邮件
	       href="mailto:xxx"
	    3、返回页面顶部空链接
	       href="#"
	    4、链接到JS
	       href="javascript:"
===============================
1、链接 - 锚点
   1、作用
      锚点：负责在页面中做记号，允许用户通过超链接随时随地跳转到这个记号上来。
   2、使用方式
      1、定义锚点 - 在页面上做记号
         1、<a>标记 name 属性
	    <a name="锚点名称">内容</a>
	 2、任何一个标记的id属性
	    <div id="锚点名称">内容</div>
      2、链接到锚点 - 跳转到记号上
         本页面锚点跳转：
         <a href="#锚点名称"></a>
	 其他页面锚点跳转：
	 <a href="页面url#锚点名称"></a>
2、表格
   1、表格的作用
      表格是按照一定的格式来显示数据
      表格是由一些“单元格”按照 从左到右，从上到下的方式进行排列的
   2、使用表格
      1、创建表格
         1、定义表格
	    <table></table>
	 2、创建 表格 行
	    <tr></tr>
	 3、创建 列(单元格)
	    <td></td>
	    所有的数据内容必须存放在 td 中
	 注意：在html中，默认情况下，每行的td的数量都是统一化的。

	 练习：创建一个 3行4列的表格，每个单元格中随意输入内容... ...
      2、表格常用属性
         1、table 元素
	   1、width
	      宽度，默认以px为单位
	   2、height
	      高度，默认以px为单位
	   3、align
	      设置表格的水平对齐方式
	      表格在其父层容器中的水平对齐方式
	      取值：
	        left，center，right
	   4、border
	      设置边框宽度，0为无边框
	   5、cellpadding
	      内边距：td中的内容与td之间的距离
	   6、cellspacing 
	      外边距：td与td之间的距离
	      取值为0，则无外边距
	   7、bgcolor
	      表格的背景颜色
	 2、tr 元素
	   1、align
	      设置 一行数据的 水平对齐方式
	   2、valign
	      设置 一行数据的 垂直对齐方式
	      取值：
	        top : 上对齐
		middle : 居中对齐
		bottom : 下对齐
	   3、bgcolor
	      背景颜色
	 3、td 元素
	   1、width ：宽度
	      注意：设置好宽度后，那么每行该列的宽度，都与设置的值相等
	   2、height ：高度
	      注意：设置高高度后，那么该行的所有列的高度，都与设置的值相等
	   3、align
	   4、valign
	   5、colspan
	      跨列
	   6、rowspan
	      跨行
   3、表格标题
      作用：定义表格标题，显示在表格内容区域之外，并且水平居中
      标记：<caption>标题内容</caption>
   4、行/列 标题
      <th></th>
      使用 方式 与 td 相同
   5、表格的复杂应用
      1、行分组
         1、表头
	    <thead></thead>
	 2、表主体
	    <tbody></tbody>
	 3、表尾
	    <tfoot></tfoot>

	 注意：默认情况下，表格中所有的 tr 都会放在一个tbody 中
      2、不规则表格
         通过 td 的两个属性 完成不规则表格的实现
	 1、colspan
	    跨列：从指定单元格开始，横向向右 合并掉几个单元格(包含自己)
	 2、rowspan
	    跨行：从指定单元格开始，纵向想下 合并掉几个单元格(包含自己)
      3、表格嵌套
         被嵌套的表格必须放在td中
      4、注意
         table:
	   display:table
	 不是块级(block)，也不是行内(inline)
	 特点：
	   1、前后自动换行(独占一行)
	   2、宽度自适应(由内容决定)

	 块级：
	   display:block
	   特点：
	     1、独占一行
	     2、宽度  100%
	     3、高度 自适应
	 行内：
	   display:inline
	   特点：  
	     1、一行内显示多个
	     2、宽度高度 自适应
	     3、对 align 无效
3、列表
   1、作用
      将 具有相似特征或前后顺序的元素，按照从上到下(从左到右)的顺序依次排列
   2、使用列表
      1、分类
         1、有序列表
	 2、无序列表
      2、语法
         列表类型：
	   无序：<ul></ul> --> Unorder List
	   有序：<ol></ol> --> Order List
	 列表项：
	   <li></li> --> List Item
	 eg:
	   <ul>
		<li>黑猫警长</li>
		<li>大力水手</li>
	   </ul>
      3、有序列表
         1、语法
	    <ol>
	      <li></li>
	      <li></li>
	      ...
	    </ol>
	 2、属性
	    1、type
	       有序列表的类型
	       取值：
	         1 ：数字（默认）
		 a ：小写英文字符
		 A ：大写英文字符
		 i ：小写罗马数字
		 I ：大写罗马数字
            2、start
	       起始编号
      4、无序列表
         语法：
	   <ul>
		<li></li>
		...
	   </ul>

	 属性：
	   type
	     取值：
	       disc : 实心圆
	       circle : 空心圆
	       square : 实心方块
	       none : 不显示标识
      5、列表嵌套
         列表项中 嵌套 一个列表
   3、定义列表
      1、作用
         给出一类事物的定义的情形

	 html5
	     html5是html中的最新版本
      2、语法
         <dl></dl> : 声明一个定义列表
	   <dt></dt>:定义要描述的名词/事物
	   <dd></dd>:对上述名词或事物的一个解释
      3、使用场合
         图文并茂
4、结构标记
   1、什么是结构标记
      w3c所提出的 新的用来表示布局的标记
      可以通过 结构标记 取代div的布局作用
   2、结构标记
      1、<header>元素
         语法：<header></header>
	 作用：定义整个文档或者某内容区域的"页眉"。定义页面顶部信息（最上方显示的内容）
	 特点：允许出现多次
	 取代：<div id="header"></div>
      2、<nav>元素
         语法：<nav></nav>
	 作用：表示页面的导航内容
	 取代：<div id="nav"></div>
      3、<section>元素
         语法：<section></section>
	 作用：表示页面主体内容
	 取代：<div id="main"></div>
      4、<article>元素
         语法：<article></article>
	 作用：表示 论坛中的一个帖子、报纸中的一篇文章、博客条目、微博条目、用户评论等
      5、<footer>元素
         语法：<footer></footer>
	 作用：表示页面中偏底部信息比如：网站备案号、解释说明
	 取代：<div id="footer"></div>
      6、<aside>元素  
         语法：<aside></aside>
	 作用：表示网页中任何一个位置的边栏。
	 取代：<div id="left_side">
      注意：如果有的内容无法使用结构标记进行展示的话，那么就是用 <div></div>


******************************************
				    第三天


1、回顾
   1、锚点
      1、什么是锚点
         在网页中某个位置做个记号
      2、使用锚点
         1、声明锚点
	    1、a 标记 name 属性
	    2、任何标记 id 属性
	 2、跳转到锚点
	    <a href="#锚点"></a>
	    <a href="页面URL#锚点"></a>
   2、表格
      1、什么是表格
         按照一定的格式来显示数据
	 由 单元格 按照 从左到右 从上到下的方式进行排列的
      2、表格标记
         表格 : <table></table>
	 表行 : <tr></tr>
	 单元格 : <td></td>
	 注意：标准化的表格每行的列数都是统一的。
      3、属性
         1、table
	    width：宽度
	    height：高度
	    align：表格在其父元素中的水平对齐方式 (left,center,right)
	    border：边框
	    cellpadding：内边距-单元格与内容之间的距离
	    cellspacing：外边距-单元格与单元格之间的距离
	    bgcolor：背景颜色
	 2、tr
	    align：默认：left
	    valign：内容的垂直对齐方式(top,middle,bottom),默认：middle
	    bgcolor:
	 3、td
	    width
	    height
	    align
	    valign
	    colspan：
	    rowspan：
      4、表格其他标记
         1、标题
	    <caption></caption>
	 2、行/列 标题
	    <th></th>
	    等同于 <td></td>
      5、表格复杂应用
         1、行分组
	    1、表头
	       <thead></thead>
	    2、表主体
	       <tbody></tbody>
	    3、表尾
	       <tfoot></tfoot>
	    注意：如果不手动编写行分组的话，在大多数的浏览器中，会自动增加tbody
	 2、不规则表格
	    通过 跨行 或 跨列 完成不规则表格的创建
	    td 属性：
	      colspan:跨列，从当前单元格处开始，横向向右合并几个单元格，包含自己
	      rowspan:跨行，从当前单元格处开始，纵向向下合并几个单元格，包含自己

	      注意：被合并掉的单元格，一定要删除，否则会产生额外的单元格影响整个表格的结构
	 3、嵌套表格
	    被嵌套的表格应该放在<td></td>中

	 4、display
	    display:table
	    特点：
	      1、前后会自动换后
	      2、宽度高度 自适应
   3、列表
      1、什么是列表
         按照一定的格式显示数据
	 由 列表项(li) 按照从上到下(从左到右)的方式进行排列的。
      2、列表的使用
         1、语法
	    组成：
	      1、列表类型
	         1、有序列表
		    <ol></ol>
		 2、无序列表
		    <ul></ul>
	      2、列表项
	         <li></li>
	 2、有序列表
	    1、属性
	       1、type 
	          作用：类型，列表项前显示的标识
		  取值：
		    1 : 默认值
		    a :
		    A :
		    i :
		    I :
	       2、start  
	          作用：起始的编号
         3、无序列表
	    1、属性
	       1、type
	          取值：
		    disc ：默认值，实心圆
		    circle ：空心圆
		    square : 实心矩形
		    none : 不显示
      3、定义列表
         1、语法
	    <dl></dl> 定义列表
	      <dt></dt> 描述的事物等
	      <dd></dd> 对描述物的解释
	 2、使用场合
	    图为并茂
   4、结构标记
      1、作用
         为了取代布局中的 div 标记，提升标签的语义性
      2、结构标记
         1、header
	    作用：页面或某区域的顶部信息
	 2、nav
	    作用：描述页面的导航
	 3、section
	    作用：描述页面的主体内容部分
	 4、aside
	    作用：描述页面中的侧边栏
	 5、footer
	    作用：描述页面偏底部的信息
	 6、article
	    作用：描述页面中 偏文本性的内容(博客条目，微博，文章内容，帖子的信息和回复等)
************************************
1、表单(重点、难点)
   1、表单的作用
      用于 显示、收集、提交用户信息到服务器上

      表单的组成：
        1、表单元素
	2、表单的控件元素
   2、表单
      1、语法
         <form></form>
	 特点：
	   看不见，摸不到
	 作用：
	   负责将 表单中出现的 表单控件元素的数据 进行提交。
	   如果 表单控件元素不再 表单 中，那么数据将无法提交
      2、属性(重点，难点)
         1、id
	    定义表单在页面中的唯一标识
	 2、name 
	    定义表单的名称-JS中使用
	 3、action
	    作用：处理 表单数据的 服务器端处理程序的 地址。
	    通常都是由服务器端处理人员提供
	    默认值 为提交到本页
	 4、method
	    作用：表单数据提交的方式
	    取值：
	      1、get
	         默认值
		 特点：以明文的方式进行提交,数据会显示在地址栏上
		   1、安全性较低(明文)
		   2、有长度限制 2KB
		 使用场合：
		   1、向服务器要数据的时候使用get提交方式
	      2、post
	         特点：以密文的方式进行数据的提交，所提交的数据不会显示在地址栏上
		   1、安全性较高
		      适合提交用户的数据
		      密码必须用post
		   2、无大小限制
		      大文件提交
		      大数据提交
		  使用场合：
		    1、提交数据让服务器去处理的时候，使用post

         5、enctype
	    作用：指定表达数据进行编码的方式(将什么样的数据提交个服务器)
	    取值：
	      1、application/x-www-form-urlencoded(默认值)
	      允许 将提交数据中的  普通字符以及 特殊字符(?,=,&) 一并提交给服务器
	      2、multipart/form-data
	         允许将 文件 进行上传
		 文件上传时，enctype的值必须为multipart/form-data
	      3、text/plain
	         只允许将普通字符提交给服务器，特殊字符不可以
   3、表单控件
      1、作用
         提供 一组 允许 用户操作的控件，从而接收用户输入的数据
      2、表单控件元素
         1、input 元素
	    文本输入:文本框，密码框
	    按钮:提交按钮，重置按钮，普通按钮
	    单选按钮：
	    复选框：
	    文件选择框：
	    隐藏域：
	 2、多行文本域-textarea
	 3、选项框-select、option
	 4、其他元素
      3、input 元素
         1、语法
	    <input> 或 <input/>
	 2、属性
	    1、type
	       根据不同的type值，创建不同的input控件
	    2、value
	       控件的值,提交给服务器时使用
	    3、name
	       控件的名称，服务器使用
	    4、disabled
	       禁用控件
	       无需给值
         3、文本框与密码框
	    1、文本框
	       <input type="text">
	    2、密码框
	       <input type="password">
	    属性：
              1、maxlength
	         限制输入的字符数
	      2、readonly
	         只读
	    3、注意
	      1、type 取值
	         type 默认或者写错的时候都是 文本框
	      2、name 取值 命名规范
	         匈牙利 命名法
		 控件缩写+作用
		 缩写：
		   文本框：txt
		   密码框：txt
		 作用名称：
		   首字符大写
		   Username
		 txtUsername
		 txtUserpwd
         4、单选按钮和复选框
	    1、单选按钮
	       <input type="radio">
	       缩写：rdo
	    2、复选框
	       <input type="checkbox">
	       缩写：chk
	    属性：
	      1、name
	         除表示名称之外，还表示分组信息。一组单选按钮或复选框，名称必须相同

	      2、value
	         必须设置
	      3、checked
	         设置默认被选中
		 无需设置值
         5、按钮
	    缩写：btn
	    1、提交按钮
	       <input type="submit">
	       作用：提交表单
	    2、重置按钮
	       <input type="reset">
	       作用：恢复到初始化状态
	    3、普通按钮
	       <input type="button">
	       作用：用户自定义效果
	    属性：
	      1、name
	      2、value
	         设置按钮上的文字
         6、隐藏域和文件选择框
	    1、隐藏域
	       <input type="hidden">
	       作用：将 一些 不想用户看的数据，但是要提交给服务器的数据 放在隐藏域 中
	    2、文件选择框
	       <input type="file">
	       注意：
	         1、method 必须为 post
		 2、enctype 值必须为 multipart/form-data
      4、选项框
         1、分类
	    下拉选项框
	    滚动列表
	 2、语法
	    <select></select> -- 创建选项框
	    <option></option> --创建选项框中的选项
	 3、属性
	    1、<select>
	       缩写：sel
	       1、name
	       2、size
	          大于1 ， 则为滚动列表
	       3、multiple
	          设置多选(滚动列表使用居多)
	    2、<option>
	       1、value
	       2、selected
	          预选中            
      5、多行文本域
         1、作用
	    多行的 文本框(text)
	 2、语法
	    <textarea></textarea>
	 3、属性
	    1、name  
	       名称
	    2、cols
	       指定文本区域的列数
	       文本域中，一行允许显示多少个字符
	       cols=50
	    3、rows
	       指定文本区域的行数
	       默认能够显示几行数据
	    4、readonly
      6、<label>元素
         1、作用
	    关联 文本 与 表单元素
	 2、语法 
	    <label>文本</label>
	    属性：
	     1、for
	        表示 与该元素相关联的表单控件的"ID值"
      7、为控件分组
         语法：
	   <fieldset></fieldset> --分组
	   <legend></legend> --分组标题
2、其他标记
   1、浮动框架(重点)
      1、作用
         将一个网页 嵌入在 另外一个网页中
      2、语法
         <iframe></iframe>
      3、属性
         1、src
	    浮动框架中的网页 url
	 2、width
	 3、height
	 4、frameborder
	    值为0时，表示无边框
   2、摘要与细节
      1、作用
         将网页中的某部分信息进行展开或收缩
      2、语法
         <details></details> --定义细节
	 <summary></summary> --定义标题
   3、度量元素
      1、语法
         <meter>文本</meter>
      2、属性
         1、min
	    度量范围的最小值，默认为0
	 2、max
	    度量范围的最大值，默认为1
	    通常会将其改为100
	 3、value
	    元素上所显示的度量值，默认为0
   4、时间元素
      1、作用
         用来 关联 时间的不同表现方式
      2、语法
         <time>显示文本</time>
      3、属性
         1、datetime
	    关联的 日期时间值
	    日期和时间之间用"T"来分割 
   5、高亮元素
      1、以突出的背景颜色 显示内容
      2、语法
         <mark>文本</mark>
   6、按钮元素
      语法
        <button>显示的文本</button>
      注意：
        1、如果将  button 放在 form中，那么作用就是 提交按钮 
	2、如果将 button 放在 form 之外，那么作用就是普通按钮 


xxx.php?txtUsername=sanfeng.zhang&txtUserpwd=1234567&rdoGender=F&chkHobby=drink&chkHobby=happy

xxx.php?txtUsername=sanfeng.zhang&txtUserpwd=&rdoGender=M&txtUserID=10010

02-表单所有元素.html?txtName=mary&rdoGender=M&selEdu=1&selCity=NJ&txtDesc=无&chkPublic=on


















